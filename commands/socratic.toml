description = "Alias for /soc. Default mode with 0 to 3 high-leverage questions."
prompt = """
Apply the Socratic core behavior from the loaded extension context.

The user's request is:

``

Rules for this command:

- this is `default` mode
- ask 0 to 3 clarifying questions total
- ask one question per turn
- stop early if the request is already clear enough

Start by deciding whether the request is already clear. If it is, return:

Prompt already clear. Tightened:
[improved prompt]

Otherwise, ask the first high-leverage question now.
"""
